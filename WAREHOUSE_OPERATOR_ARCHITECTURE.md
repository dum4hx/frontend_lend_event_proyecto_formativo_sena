#!/usr/bin/env node
/**\n * Warehouse Operator Module Architecture\n *\n * This document explains the high-level architecture and data flow\n * of the warehouse operator module.\n */\n\n// ============================================================================\n// AUTHENTICATION FLOW\n// ============================================================================\n\nconst authFlow = `\n1. INVITATION STAGE\n   Admin → Invites warehouse operator via /admin/team\n   System → Sends email with activation link\n   Email → Contains: /accept-invite?email=...&token=...\n\n2. ACCOUNT ACTIVATION\n   User → Clicks email link → /accept-invite page loads\n   User → Sets password (must meet requirements)\n   System → Validates token and password\n   System → Creates/updates user account\n   User → Redirected to /login (success)\n\n3. LOGIN\n   User → Enters email and password\n   System → Validates credentials\n   System → Generates JWT token\n   System → Stores token in localStorage/cookie\n   System → Calls checkAuth() to get user info\n   System → Determines role from user.role === 'warehouse_operator'\n   System → Uses getDashboardUrlByRole() → returns '/warehouse-operator'\n   User → Redirected to /warehouse-operator\n\n4. DASHBOARD ACCESS\n   System → RequireRole guard checks user.role\n   Guard → Verifies role is 'warehouse_operator'\n   Guard → If matched: loads WarehouseOperatorLayout\n   Guard → If not matched: redirects to /unauthorized\n   User → Sees warehouse operator dashboard\n\n5. SESSION MANAGEMENT\n   Token → Stored client-side\n   Token → Sent with every API request\n   Token → Automatically refreshed before expiring\n   Logout → Clears token and redirects to /login\n`;\n\n// ============================================================================\n// COMPONENT HIERARCHY\n// ============================================================================\n\nconst componentHierarchy = `\nApp\n├── AuthProvider (context wrapper)\n│   ├── BrowserRouter\n│   │   └── Routes\n│   │       ├── Public Routes (/login, /signup, etc.)\n│   │       │\n│   │       ├── /warehouse-operator (Protected)\n│   │       │   └── RequireRole ['warehouse_operator']\n│   │       │       └── WarehouseOperatorLayout (Suspense wrapped)\n│   │       │           ├── Sidebar\n│   │       │           │   ├── NavLink items\n│   │       │           │   ├── Logo/branding\n│   │       │           │   ├── Navigation\n│   │       │           │   └── Logout button\n│   │       │           │\n│   │       │           └── Outlet (page content)\n│   │       │               ├── Dashboard\n│   │       │               │   ├── StatCard (x4)\n│   │       │               │   ├── Recent movements section\n│   │       │               │   └── Low stock items section\n│   │       │               │\n│   │       │               ├── Inventory\n│   │       │               │   ├── Search bar\n│   │       │               │   ├── Items table\n│   │       │               │   └── CRUD buttons\n│   │       │               │\n│   │       │               ├── Locations\n│   │       │               │   ├── Search bar\n│   │       │               │   ├── Location cards\n│   │       │               │   └── Capacity bars\n│   │       │               │\n│   │       │               ├── StockMovements\n│   │       │               │   ├── Search bar\n│   │       │               │   ├── Type filter\n│   │       │               │   └── Movements table\n│   │       │               │\n│   │       │               ├── Alerts\n│   │       │               │   ├── Stats cards\n│   │       │               │   ├── Status filter\n│   │       │               │   └── Alert list\n│   │       │               │\n│   │       │               └── Settings\n│   │       │                   ├── Personal info form\n│   │       │                   ├── Language select\n│   │       │                   ├── Notification toggles\n│   │       │                   └── Save button\n│   │       │\n│   │       └── Other modules (/admin, /super-admin, etc.)\n│   │           └── Protected similarly\n`;\n\n// ============================================================================\n// DATA FLOW\n// ============================================================================\n\nconst dataFlow = `\nUSER INTERACTIONS\n└─ useState (local component state)\n   └─ setSearchTerm, setFormData, etc.\n\nAPI COMMUNICATION\n└─ Service functions (warehouseOperatorService.ts)\n   ├─ get() / post() / patch() / del() from lib/api.ts\n   └─ Returns: ApiSuccessResponse<T>\n       └─ Contains: status, data, message?\n\nGLOBAL STATE\n└─ AuthContext\n   ├─ user: User | null\n   ├─ isLoggedIn: boolean\n   ├─ isLoading: boolean\n   └─ checkAuth(): Promise<User | null>\n\nROUTING\n└─ React Router\n   ├─ Matches URL to route pattern\n   ├─ Renders matched component\n   ├─ RequireRole guard checks auth\n   └─ Redirects if access denied\n`;\n\n// ============================================================================\n// STATE MANAGEMENT PATTERN\n// ============================================================================\n\nconst stateManagement = `\n// Example from Dashboard.tsx\n\nconst [stats, setStats] = useState({\n  totalItems: 0,\n  lowStockAlerts: 0,\n  activeLocations: 0,\n  stockMovementsToday: 0,\n});\nconst [loading, setLoading] = useState(true);\nconst [error, setError] = useState<string | null>(null);\n\nPATTERN:\n1. Initialize state with default values\n2. Set loading = true when fetching\n3. Fetch data via service function\n4. Update state with response\n5. Handle errors with try/catch\n6. Set loading = false in finally block\n7. Render based on state (loading ? spinner : content, error ? error : content)\n`;\n\n// ============================================================================\n// SERVICE FUNCTION PATTERN\n// ============================================================================\n\nconst serviceFunctionPattern = `\n// From warehouseOperatorService.ts\n\nexport async function getInventoryItems(\n  params?: { page?: number; limit?: number }\n): Promise<ApiSuccessResponse<InventoryListResponse>> {\n  return get<InventoryListResponse>(\n    \"/warehouse/inventory\",\n    params as Record<string, string | number | boolean | undefined>\n  );\n}\n\nPATTERN:\n1. Function parameters are optional options object\n2. Return type is Promise<ApiSuccessResponse<T>>\n3. T is the data shape (InventoryListResponse)\n4. Calls get/post/patch/del from lib/api.ts\n5. Path is relative to API base URL\n6. Parameters are spread into the query string\n`;\n\n// ============================================================================\n// TYPE SYSTEM\n// ============================================================================\n\nconst typeSystem = `\n// Core types in types/api.ts\n\nUserRole = \"super_admin\" | \"owner\" | \"manager\" | \n           \"warehouse_operator\" | \"commercial_advisor\"\n\nUser = {\n  id: string\n  email: string\n  name: PersonName { firstName, firstSurname, ... }\n  role: UserRole\n  status: \"active\" | \"inactive\" | \"invited\" | \"suspended\"\n  phone?: string\n}\n\n// Service-specific types in warehouseOperatorService.ts\n\nInventoryItem = {\n  id: string\n  sku: string\n  name: string\n  quantity: number\n  minThreshold: number\n  location: string\n  lastUpdated: string\n}\n\nWarehouseAlert = {\n  id: string\n  type: \"critical\" | \"warning\" | \"info\"\n  title: string\n  description: string\n  location?: string\n  itemSku?: string\n  timestamp: string\n  status: \"active\" | \"acknowledged\" | \"resolved\"\n}\n\n// API Response wrapper\n\nApiSuccessResponse<T> = {\n  status: \"success\"\n  data: T\n  message?: string\n}\n`;\n\n// ============================================================================\n// ROUTING RULES\n// ============================================================================\n\nconst routingRules = `\nFROM LOGIN\n├─ user.role === \"super_admin\"\n│  └─ navigate(\"/super-admin\")\n│\n├─ user.role === \"warehouse_operator\"\n│  └─ navigate(\"/warehouse-operator\")\n│\n├─ user.role === \"owner\" | \"manager\" | \"commercial_advisor\"\n│  ├─ Check subscription status\n│  └─ navigate(\"/admin\")\n│\n└─ Unknown role\n   └─ navigate(\"/\") default\n\nTHIS IS IMPLEMENTED IN:\n├─ getDashboardUrlByRole(role) - src/utils/roleRouting.ts\n├─ Login.tsx - uses it to redirect\n└─ roleGuard.tsx - RequireRole component enforces it\n`;\n\n// ============================================================================\n// API CALL FLOW\n// ============================================================================\n\nconst apiCallFlow = `\nComponent Event\n  ↓\nComponent calls service function\n  e.g., getInventoryItems({ page: 1, limit: 20 })\n  ↓\nService function calls get(path, params)\n  from ../lib/api.ts\n  ↓\ngetrequest() wrapper handles:\n  ├─ Headers: Content-Type, Authorization (JWT)\n  ├─ Credentials: include (for cookies)\n  ├─ Retry logic (with exponential backoff)\n  ├─ Token refresh on 401\n  └─ Error transformation\n  ↓\nResponse received\n  ↓\nconvert to ApiSuccessResponse or ApiError\n  ↓\nReturn to component\n  ↓\nComponent updates state with response\n  ↓\nComponent re-renders with new data\n`;\n\n// ============================================================================\n// ERROR HANDLING\n// ============================================================================\n\nconst errorHandling = `\n// In components:\n\ntry {\n  const response = await getInventoryItems();\n  setItems(response.data.items);\n  setError(null);\n} catch (err: unknown) {\n  const message = \n    err instanceof ApiError \n      ? err.message \n      : \"Unknown error\";\n  setError(message);\n} finally {\n  setLoading(false);\n}\n\n// ApiError class from lib/api.ts\nApiError = {\n  message: string\n  statusCode: number\n  code?: string\n  details?: Record<string, unknown>\n}\n\nCOMMON STATUS CODES:\n- 401: Unauthorized (token missing/invalid)\n- 403: Forbidden (insufficient permissions)\n- 404: Not Found (resource doesn't exist)\n- 500: Server Error\n`;\n\n// ============================================================================\n// STYLING APPROACH\n// ============================================================================\n\nconst styling = `\nFRAMEWORK: Tailwind CSS\n\nCOLOR PALETTE:\n- Primary: #FFD700 (gold) - active/hover states\n- Dark: #121212 - main background\n- Darker: #1a1a1a - card backgrounds\n- Border: #333 - subtle dividers\n- Text: white, gray-400, gray-500\n\nCOMMON CLASSES:\n- bg-[#121212] - card background\n- border border-[#333] - card border\n- text-white - primary text\n- text-gray-400 - secondary text\n- hover:border-[#FFD700] - active state\n- rounded-[12px] - card radius\n- px-4 py-3 - padding\n- space-y-4 - vertical spacing\n- grid grid-cols-1 md:grid-cols-2 - responsive grid\n\nLUCIDE ICONS:\n- Size: 20-32px\n- Color: inherited or text-[#FFD700]\n- Usage: LayoutDashboard, Package, MapPin, AlertCircle, etc.\n`;\n\n// ============================================================================\n// RESPONSIVE DESIGN\n// ============================================================================\n\nconst responsiveDesign = `\nBREAKPOINTS (Tailwind):\n- Mobile: < 768px (default)\n- Tablet: md: 768px+\n- Desktop: lg: 1024px+\n\nLAYOUT ADJUSTMENTS:\n- Sidebar: fixed left-0 (always visible)\n- Main content: ml-64 (margin-left for sidebar)\n- Grids: grid-cols-1 md:grid-cols-2 lg:grid-cols-4\n- Tables: overflow-x-auto on mobile\n- Forms: full-width on mobile, narrower on desktop\n\nTOUCH TARGETS:\n- Minimum 44x44px for buttons\n- Adequate padding for touch interaction\n- No hover-only information\n`;\n\nconsole.log('Warehouse Operator Module Architecture Documentation');\nconsole.log('\\n' + authFlow);\nconsole.log('\\n' + componentHierarchy);\nconsole.log('\\n' + dataFlow);\nconsole.log('\\n' + stateManagement);\nconsole.log('\\n' + serviceFunctionPattern);\nconsole.log('\\n' + typeSystem);\nconsole.log('\\n' + routingRules);\nconsole.log('\\n' + apiCallFlow);\nconsole.log('\\n' + errorHandling);\nconsole.log('\\n' + styling);\nconsole.log('\\n' + responsiveDesign);\n